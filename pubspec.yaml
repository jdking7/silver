name: ongojishin
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Firebase
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2
  firebase_auth: ^6.1.4
  firebase_storage: ^13.0.6
  # State Management
  provider: ^6.1.1
  # Utilities
  intl: ^0.20.2
  cached_network_image: ^3.3.1 # For caching images
  url_launcher: ^6.2.2 # For launching external links if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Asset definitions will be added later when we have assets
  # assets:
  #   - assets/images/
